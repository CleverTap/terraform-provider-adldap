{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "goreleaser",
            "type": "shell",
            "command": "goreleaser --skip-publish --rm-dist",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "goreleaser (snapshot)",
            "type": "shell",
            "command": "goreleaser --snapshot --skip-publish --rm-dist",
            "problemMatcher": [],
            "group": "build"
        }
    ]
}